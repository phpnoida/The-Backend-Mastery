// ════════════════════════════════════════════════════════════════════
// 09 — MATHS
// Run with Quokka. Write your answer under each question and console.log it.
// "Expected" is your self-check. No solution code is given — derive it.
// ════════════════════════════════════════════════════════════════════

// ── Q01 ──────────────────────────────────────────────
// Round, floor, and ceil the value.
// Expected: 4  3  4
const q01Val = 3.6;

// your answer ↓


// ── Q02 ──────────────────────────────────────────────
// Max and min of the list (hint: spread into Math.max / Math.min).
// Expected: 90  12
const q02Nums = [45, 90, 12, 33];

// your answer ↓


// ── Q03 ──────────────────────────────────────────────
// Random integer between 1 and 6 (inclusive) — a dice roll.
// Expected: (a whole number 1–6; will differ each run)

// your answer ↓


// ── Q04 ──────────────────────────────────────────────
// What percent is `got` of `total`? Round to a whole number.
// Expected: 75
const q04Got = 30;
const q04Total = 40;

// your answer ↓


// ── Q05 ──────────────────────────────────────────────
// Modulo: is the number EVEN? log true/false.
// Expected: true
const q05Num = 10;

// your answer ↓


// ── Q06 ──────────────────────────────────────────────
// Sum the array, then the average (sum / length).
// Expected: 100  25
const q06Nums = [10, 20, 30, 40];

// your answer ↓


// ── Q07 ──────────────────────────────────────────────
// Pagination math: how many pages for `totalItems` at `pageSize` per page? (round UP)
// Expected: 4
const q07TotalItems = 31;
const q07PageSize = 10;

// your answer ↓


// ── Q08 ──────────────────────────────────────────────
// Clamp the value into the range [1, 100]: 150 should become 100.
// Expected: 100
const q08Val = 150;

// your answer ↓


// ── Q09 ──────────────────────────────────────────────
// Round a price to exactly 2 decimal places and return it as a Number.
// Expected: 19.99
const q09Price = 19.985;

// your answer ↓


// ── Q10 ── (mini real-world) ─────────────────────────
// Format a paise/cents amount (integer) as currency: 123456 → "₹1234.56".
// Expected: "₹1234.56"
const q10Cents = 123456;

// your answer ↓
