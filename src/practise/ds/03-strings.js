// ════════════════════════════════════════════════════════════════════
// 03 — STRINGS
// Run with Quokka. Write your answer under each question and console.log it.
// "Expected" is your self-check. No solution code is given — derive it.
// ════════════════════════════════════════════════════════════════════

// ── Q01 ──────────────────────────────────────────────
// Convert to lowercase, then to UPPERCASE.
// Expected: hello world  HELLO WORLD
const q01Str = "Hello World";

// your answer ↓


// ── Q02 ──────────────────────────────────────────────
// Trim the surrounding whitespace.
// Expected: "amit@x.com"   (length 10, no spaces)
const q02Email = "   amit@x.com   ";

// your answer ↓


// ── Q03 ──────────────────────────────────────────────
// Split the CSV string into an array.
// Expected: ["red", "green", "blue"]
const q03Csv = "red,green,blue";

// your answer ↓


// ── Q04 ──────────────────────────────────────────────
// Join the array into a single comma-separated string.
// Expected: "node,express,mongo"
const q04Tags = ["node", "express", "mongo"];

// your answer ↓


// ── Q05 ──────────────────────────────────────────────
// Does the email contain "@"? Does it END with ".com"?
// Expected: true  true
const q05Email = "sara@example.com";

// your answer ↓


// ── Q06 ──────────────────────────────────────────────
// Replace all hyphens with spaces.
// Expected: "the backend mastery"
const q06Slug = "the-backend-mastery";

// your answer ↓


// ── Q07 ──────────────────────────────────────────────
// Capitalize the first letter, keep the rest as-is.
// Expected: "Amit"
const q07Name = "amit";

// your answer ↓


// ── Q08 ──────────────────────────────────────────────
// Slugify: lowercase + replace spaces with hyphens.
// Expected: "hello-backend-world"
const q08Title = "Hello Backend World";

// your answer ↓


// ── Q09 ──────────────────────────────────────────────
// Reverse the string.
// Expected: "tpircsavaj"
const q09Str = "javascript";

// your answer ↓


// ── Q10 ──────────────────────────────────────────────
// Count how many times the letter "a" appears.
// Expected: 4
const q10Str = "abracadabra";

// your answer ↓


// ── Q11 ──────────────────────────────────────────────
// Capitalize the first letter of EACH word (title case).
// Expected: "The Quick Brown Fox"
const q11Sentence = "the quick brown fox";

// your answer ↓


// ── Q12 ── (mini real-world) ─────────────────────────
// Mask an email: keep the first char + domain, hide the rest of the local part with "***".
// Expected: "a***@x.com"
const q12Email = "amit@x.com";

// your answer ↓
